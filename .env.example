# Hostname or IP address that the Rocket.Chat container should use to reach
# the MongoDB instance running on your host machine.
#
# For Docker Desktop, host.docker.internal is the portable default.
# If your MongoDB replica set was initialized with a specific host IP, set this
# to that exact IP instead, for example: 192.168.56.1
MONGO_HOST=host.docker.internal
